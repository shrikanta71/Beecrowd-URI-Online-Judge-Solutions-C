// beecrowd | 2755 - Output 9

#include<stdio.h>

int main()
{
    printf("\"Ro'b'er\tto\\/\"\n"); // To use "" we have to put \ before them and to use \ also put another \ before it.
    printf("(._.) ( l: ) ( .-. ) ( :l ) (._.)\n");
    printf("(^_-) (-_-) (-_^)\n");
    printf("(\"_\") ('.')\n"); // To use "" we have to put \ before them

    return 0;
}
